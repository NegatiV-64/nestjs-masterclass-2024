export enum TicketsSortBy {
  TicketName = 'ticketName',
  TicketPrice = 'ticketPrice',
  TicketQuantity = 'ticketQuantity',
  TicketCreatedAt = 'ticketCreatedAt',
}

export enum TicketsSortOrder {
  Asc = 'asc',
  Desc = 'desc',
}
